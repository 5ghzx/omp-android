# OMP Android

Native Android guest client for Oh My Pi `/collab` sessions.

Supports full-link prompt/interrupt access, view-only links, live transcript events, encrypted AES-256-GCM frames, and the OMP v3 wire protocol.
